{# templates/emprunt_livre/new.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Ajouter un emprunt de livre{% endblock %}

{% block body %}
    <h1>Ajouter un emprunt de livre</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Ajouter</button>
    {{ form_end(form) }}

    <a href="{{ path('emprunt_livre_index') }}">Retour à la liste</a>
{% endblock %}
